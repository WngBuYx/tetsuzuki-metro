# UI 実装仕様書 — てつづきメトロ

> 対象読者: HANDOFF.md を読んだうえで UI を実装する開発者。
> このファイルは画面設計・コンポーネント構成・インタラクション仕様を定義する。
> エンジン API は HANDOFF.md §1 を参照。

---

## 0. 技術スタック

| 項目 | 選定 | 理由 |
|---|---|---|
| フレームワーク | **Vanilla JS (ESM)** | 依存ライブラリゼロの方針を維持。route.js がそのまま import できる |
| 路線図 | **インライン SVG** (DOM 直接生成) | Canvas は印刷・アクセシビリティに弱い。D3 不要 — 駅数 ≤70 で手書き座標計算のほうが制御しやすい |
| 状態管理 | **URL ハッシュ** のみ | サーバに一切送らない。`hashchange` イベントで画面遷移 |
| CSS | **単一 CSS ファイル** (CSS custom properties) | ライトモード固定（高齢者向け高コントラスト優先） |
| ホスティング | Cloudflare Pages | 静的ファイルのみ |
| 地図 | 不要（時間がなければ切り捨て。住所・電話のテキストで代替） |

### ファイル構成（新規作成分）

```
tetsuzuki_metro/
├─ index.html              エントリポイント
├─ css/
│  └─ style.css            全スタイル（印刷用 @media print 含む）
├─ js/
│  ├─ app.js               ルーター・状態管理・画面切替
│  ├─ screens/
│  │  ├─ entry.js           シナリオ選択
│  │  ├─ questions.js       質問ウィザード
│  │  ├─ trips.js           ★跑腿プランナー（メイン画面）
│  │  ├─ trip-detail.js     1つの行き先の詳細展開
│  │  ├─ routemap.js        路線図 SVG 描画（副画面）
│  │  └─ station-detail.js  駅詳細パネル（路線図から開く）
│  ├─ components/
│  │  ├─ svg-station.js     SVG 駅マーカー生成
│  │  ├─ lang-toggle.js     言語切替
│  │  └─ clear-button.js    回答消去
│  └─ i18n.js               UI ラベルの3言語テキスト
├─ src/route.js             （既存・変更不要）
└─ data/*.json              （既存・変更不要）
```

---

## 1. 設計思想

### 1.1 核心原則: 「どこに行って何をする」が主、「いつまでに」は副

ユーザーの本質的な問い:
> 「明日どこへ行って、何を持っていけばいい？」

路線図（時間軸）はこの問いに直接答えない。ユーザーが必要としているのは **行き先ごとの跑腿リスト** である。

| | 跑腿プランナー（メイン） | 路線図（サブ） |
|---|---|---|
| 答える問い | どこへ・何を・何を持って | いつまでに・全体俯瞰 |
| 使う場面 | 区役所に行く前夜 | 最初に全貌を把握したいとき |
| 印刷する | Yes（A4持ち物リスト） | No |
| 審査員向け | 実用性の証明 | 「前2週間が地獄」の可視化 |

### 1.2 自治体に依存しない設計

データ層の三層構造（README §3）に対応し、UI もデフォルトは **全国共通の通称** で表示する。

| 自治体ファイルの有無 | 行き先の表示 | 窓口の表示 |
|---|---|---|
| **未選択（デフォルト）** | `window.authority`（「市区町村」「年金事務所」等） | `window.label[lang]`（「お住まいの市区町村の窓口」） |
| **選択済み（例: 新宿区）** | `municipality_name[lang]` + `authority` | `windows[stationId].dept` + `tel` + `also` |

自治体選択はオプションのステップであり、未選択でも全機能（通数計算・期限・書類リスト）が使える。
現時点で自治体ファイルは新宿区・練馬区の2件のみ。デモ時に切り替えて見せる用途。

---

## 2. 画面遷移

```
[入口] → [質問 ×N] → [跑腿プランナー ★メイン]
                          ├→ [行き先詳細]（展開/オーバーレイ）
                          ├→ [路線図]（タブ切替）
                          │    └→ [駅詳細]（オーバーレイ）
                          └→ [印刷]
```

ハッシュ形式:
- `#/` — 入口
- `#/q/{scenarioId}` — 質問ウィザード
- `#/trips/{scenarioId}?a={base64}&d={base64}` — 跑腿プランナー（メイン）
- `#/trips/{scenarioId}/t/{windowKey}` — 行き先詳細
- `#/map/{scenarioId}?a=...&d=...` — 路線図（副画面）
- `#/map/{scenarioId}/s/{stationId}` — 駅詳細
- ハッシュなし or 不正 → `#/` にリダイレクト

オプショナルパラメータ:
- `&lang=ja|easy|en` — 言語
- `&m={municipalityCode}` — 自治体（例: `131041`）。未指定＝通用表示

`answers` と `eventDates` は JSON → base64url でハッシュに載せる。

---

## 3. 共通要素

### 3.1 ヘッダー

```
┌─────────────────────────────────────────────────┐
│  てつづきメトロ               [日本語|やさしい|EN] │
└─────────────────────────────────────────────────┘
```

- ロゴ「てつづきメトロ」は常に左上。タップで `#/` に戻る
- 言語切替は常に右上。3ボタン: `日本語` / `やさしい` / `EN`

### 3.2 フッター（結果画面に表示）

```
┌─────────────────────────────────────────────────┐
│  [A4に印刷]                        [回答を消去]   │
└─────────────────────────────────────────────────┘
```

- 「回答を消去」→ 確認ダイアログ → `location.hash = ''` + `location.reload()`
- 「A4に印刷」→ `window.print()`

### 3.3 タブバー（メイン/副画面の切替）

跑腿プランナーと路線図の切替:
```
┌──────────────────────────────────┐
│  [ 跑腿プラン ]  [ 路線図 ]        │
└──────────────────────────────────┘
```

ラベル案:
- ja: 「やること」/「全体を見る」
- easy: 「やること」/「ぜんたい」
- en: 「What to do」/「Timeline」

---

## 4. 画面仕様

### 4.1 入口（シナリオ選択）

**目的**: 7つのライフイベントから1つ選ぶ。

**レイアウト**:
- 見出し: 「どのようなことがありましたか？」（18px / 500weight）
- 補足: 「回答はサーバーに送信されません」（13px / muted）
- カードグリッド: 2列（モバイルは1列）

**シナリオカード**:
```
┌───────────────────────┐
│  ご家族が亡くなられたとき  │  ← name[lang]
│  役所・保険・年金・相続…   │  ← 路線名の要約
│  最大26手続 / 8分野       │  ← scenarios.procedures.length
└───────────────────────┘
```

- 選択中: テーマカラーボーダー（2px）
- `status !== 'complete'` のシナリオは disabled + 「準備中」バッジ

**アクション**: 「次へ」ボタン → `#/q/{scenarioId}`

**データソース**: `scenarios.json` の `scenarios[]`

### 4.2 質問ウィザード

**目的**: 条件を1問ずつ聞く。未回答のまま先に進める。

**表示する質問の選定ロジック**:
1. `conditions.json` から `scenarios` に現在のシナリオ ID を含む条件を抽出
2. `tier` でソート: `profile` → `core` → `detailed`（`follow_up` は親回答後に挿入）
3. `follow_up` の条件は `parent` の回答が `when` と一致した場合のみ表示

**死別シナリオの場合（実質5問）**:
1. 亡くなった日（date input — 質問ではなく起算日入力。最初に聞く）
2. `deceased_was_head` — 世帯主でしたか？（boolean）
3. `insurance` — 健康保険の種類（choice: kokuho/kouki/shakai）
4. `nenkin_type` — 年金の種類（choice）
5. `has_real_estate` — 不動産はあるか（boolean）
+ follow_up が必要に応じて差し込まれる

**レイアウト**: Typeform 風の1問1画面。

```
┌─────────────────────────────────────┐
│  ████████░░░░░░░░░  2/5             │  ← プログレスバー
│                                     │
│  質問 2 / 5                         │
│                                     │
│  亡くなられた方の健康保険は           │  ← question[lang] (20px)
│  次のどれでしたか？                  │
│                                     │
│  ┌─ 国民健康保険（74歳まで）─────┐  │
│  └──────────────────────────────┘  │
│  ┌─ 後期高齢者医療（75歳以上）───┐  │
│  └──────────────────────────────┘  │
│  ┌─ 社会保険（会社の健康保険）───┐  │
│  └──────────────────────────────┘  │
│  ┌─ わからない ──────────────────┐  │
│  └──────────────────────────────┘  │
│                                     │
│     この質問をスキップ               │
│                                     │
│  [← 戻る]              [次へ →]     │
└─────────────────────────────────────┘
```

**type 別の入力コンポーネント**:
- `boolean` → 2択ボタン（はい / いいえ / わからない）
- `choice` → ラジオ風リスト + 「わからない」
- `number` → 数値入力 + 「わからない」
- 日付入力 → `<input type="date">` + 「まだわからない」

**「わからない」の扱い**:
- `answers` にキーを入れない（= UNKNOWN として3値論理に載る）
- 質問をスキップした場合も同様

**死亡日の特別扱い**:
- 死別シナリオでは質問の前に死亡日入力を挟む
- 入力すると `eventDates.death` にセット
- `known`（相続を知った日）と `funeral` は同日で初期化
- 「詳しく設定」を開くと `known` / `funeral` を個別入力可

**自治体選択（オプション）**:
- 質問の最後に「お住まいの市区町村を選ぶと、窓口の課名・電話番号が表示されます」
- 選択肢: 新宿区 / 練馬区 / 選ばない（現時点の2件）
- 選ばなくても全機能が使える旨を明記

**ウィザード完了**: → `#/trips/{scenarioId}?a=...&d=...`

### 4.3 跑腿プランナー ★メイン画面

**目的**: 「どこへ行って、何をして、何を持っていく」を行き先ごとに表示する。

**データソース**: `plan.windows`（`groupByWindow` の出力）

#### 4.3.1 全体レイアウト

```
┌─────────────────────────────────────────────────────┐
│  ⚠ 過ぎると取り返しがつかない期限                      │
│  相続放棄（11/1満了・あと86日）/ 準確定申告（12/1満了） │
├─────────────────────────────────────────────────────┤
│  [ やること ★ ]  [ 全体を見る ]                       │  ← タブ
├─────────────────────────────────────────────────────┤
│  [20手続]  [5か所]  [戸籍8通]  [取返不可3]             │  ← サマリー
├─────────────────────────────────────────────────────┤
│                                                       │
│  ┌─ Trip 1 ─────────────────────────────────────┐   │
│  │  ① あなたの区役所              計12件          │   │
│  │     最も早い期限: 7日（死亡届）                 │   │
│  │  ──────────────────────────────────           │   │
│  │  ● 死亡届の提出                 7日 ⚠         │   │
│  │  ● 世帯主の変更届              14日            │   │
│  │  ● 国保の資格喪失届            14日            │   │
│  │  ...ほか9件                                    │   │
│  │  ──────────────────────────────────           │   │
│  │  持っていくもの:                                │   │
│  │  戸籍謄本×3 / 死亡診断書 / 本人確認書類 / ...   │   │
│  └──────────────────────────────────────────────┘   │
│                                                       │
│  ┌─ Trip 2 ─────────────────────────────────────┐   │
│  │  ② 年金事務所                   計3件          │   │
│  │  ...                                          │   │
│  └──────────────────────────────────────────────┘   │
│                                                       │
│  ┌─ Trip 3 〜 Trip 5 ... ──────────────────────┐   │
│  └──────────────────────────────────────────────┘   │
│                                                       │
│  ┌─ 用意する書類（全行き先の合計）───────────────┐   │
│  │  戸籍謄本  8通  ¥3,600                        │   │
│  │  ...                                          │   │
│  │  ★ 法定相続情報一覧図なら無料で1回            │   │
│  └──────────────────────────────────────────────┘   │
│                                                       │
│  [A4に印刷]                            [回答を消去]   │
└─────────────────────────────────────────────────────┘
```

#### 4.3.2 Trip カード

1つの行き先（`plan.windows` の1エントリ）= 1カード。

**ヘッダー部**:
```
┌───────────────────────────────────────┐
│  ① あなたの区役所           計12件     │
│     最も早い期限: 7日（死亡届）        │
└───────────────────────────────────────┘
```

| 要素 | ソース | 自治体未選択時 | 自治体選択時（例: 新宿区） |
|---|---|---|---|
| 行き先名 | `authority` | 「あなたの区役所」 | 「新宿区役所」 |
| 補足 | `jurisdictionLabel` | （旧住所地）等。空なら非表示 | 同左 |
| 件数 | `total` | 計N件 | 同左 |
| 最も早い期限 | グループ内の `timeline` 最小 `days` | N日（手続名） | 同左 |
| バッジ | `oneTripPossible` | 「1回でまとめられる」 | 同左 |

**行き先名の表示ルール**:

自治体未選択のデフォルト表示:
- `authority === '市区町村'` → 「あなたの区役所」（ja）/「あなたの やくしょ」（easy）/「Your ward office」（en）
- `authority === '年金事務所'` → そのまま表示（全国共通の名称）
- `authority === '法務局'` → そのまま表示
- `authority === '税務署'` → そのまま表示
- `authority === '家庭裁判所'` → そのまま表示
- 民間（銀行・保険・郵便等）→ そのまま表示

自治体選択時:
- `authority === '市区町村'` → `municipality_name[lang]`（「新宿区役所」）

**手続リスト部**（デフォルト: 最大4件表示 + 「ほかN件」で折りたたみ）:
```
● 死亡届の提出                  7日 ⚠
● 世帯主の変更届               14日
○ 国保の扶養加入               14日     ← maybe（不明）
● 印鑑登録証の返納             期限なし
...ほか8件 ▼
```

- `●` = required、`○` = maybe（破線 or グレー）
- ドットの色 = 路線色
- 期限の表示: `days` + 満了日（`dueAssumed` なら「目安」と明記）
- `days <= 14` → 赤文字 + ⚠

**持ち物部**:
```
┌ 持っていくもの ──────────────────────────┐
│  戸籍謄本 ×3 / 死亡診断書 / 本人確認書類  │
│  / 国保被保険者証 / 年金手帳              │
│                                          │
│  ⚠ 提出方法が未確認の書類が2件あります    │  ← unconfirmedAt.length > 0
└──────────────────────────────────────────┘
```

この行き先で必要な書類だけを表示する（全体の合計は下の書類セクションで）。
自治体選択時は `dept` / `tel` / `also` も表示:

```
  📍 戸籍住民課 戸籍係  03-5273-3509
  📍 医療保険年金課 国保資格係
  ※ 各特別出張所でも手続き可能
```

#### 4.3.3 irreversible バナー

画面最上部に常時表示。`plan.irreversible` から生成:
- 赤背景バナー
- 手続名 + 満了日 + 残り日数を列挙
- `days <= 14` なら強調表示（太字 + アイコン）

#### 4.3.4 サマリー4セル

| セル | 値 | ソース |
|---|---|---|
| 手続の数 | `plan.counts.required + plan.counts.maybe` | 「必要」+「不明」 |
| 行き先 | `plan.windows.length` | 行く場所の数 |
| 戸籍謄本 | `plan.documents` から koseki-tohon の `copies` | |
| 取返不可 | `plan.irreversible.length` | |

#### 4.3.5 書類セクション（全行き先の合計）

Trip カード群の下に表示。`plan.documents` から生成。

```
┌─ 用意する書類（合計）─────────────────────┐
│                                            │
│  戸籍謄本 @¥450          8通    ¥3,600     │
│    原本提出8 / 提示2                        │
│                                            │
│  除籍謄本 @¥750          2〜4通  ¥1,500〜   │
│    ⚠ 要確認 2件                            │
│                                            │
│  ─────────────────────────────             │
│  合計                    ¥5,400〜6,900      │
└────────────────────────────────────────────┘
```

- `copies !== copiesMax` → 幅表示「N〜M通」
- `unconfirmedAt.length > 0` → 「要確認 N件」を警告色で表示
- `alternative` がある場合 → 提案ボックスを直下に表示

#### 4.3.6 traps（詰みの警告）

`plan.traps` があれば書類セクションの上に表示:
```
⚠ 「死亡届」の期限は7日ですが、必要な「戸籍謄本」の取得に最大7日かかります。
  書類の請求を最優先で始めてください。
```

### 4.4 路線図（副画面・タブ切替）

**目的**: 全手続の期限を時間軸で一望する。「最初の2週間が地獄」を可視化。

タブで跑腿プランナーから切り替える。審査員へのデモと、全体俯瞰を求めるユーザー向け。

#### 4.4.1 レイアウト

```
┌────┬──────┬───────┬─────────┬─────────┬──────────┬──────────┐
│    │ 7日  │ 14日  │  3か月  │ 10か月  │   2年    │ 期限なし │  ← 対数時間軸
│    │      │       │         │         │          │          │
│役所│  ●   │ ● ●   │         │         │          │  ○ ○     │
│保険│      │ ● ●   │         │         │   ●      │          │
│年金│      │       │         │         │   ●      │  ◌       │
│税金│      │       │   ●★    │  ●★     │          │          │
│相続│      │       │  ●★     │         │          │  ○ ○     │
│民間│      │       │         │         │   ●      │  ○ ○     │
│    ▼今日                                                     │
└─────────────────────────────────────────────────────────────┘
```

#### 4.4.2 SVG 座標計算

**横軸（x 座標）**: 対数スケール

```js
function xPosition(days, svgWidth) {
  const LEFT_PAD = 60;
  const RIGHT_PAD = 80;
  const CHART_W = svgWidth - LEFT_PAD - RIGHT_PAD;
  if (days == null) return svgWidth - RIGHT_PAD / 2;
  if (days <= 0) return LEFT_PAD;
  const MAX_DAYS = 730;
  const norm = Math.log10(days + 1) / Math.log10(MAX_DAYS + 1);
  return LEFT_PAD + norm * CHART_W;
}
```

**縦軸（y 座標）**: 路線ごとに固定トラック

```js
const LINES = ['yakusho','hoken','nenkin','zeimu','sozoku','minkan','zairyu','shokuba'];
const TRACK_H = 50;
const TOP_PAD = 30;
function yPosition(lineId, offset = 0) {
  const idx = LINES.indexOf(lineId);
  return TOP_PAD + idx * TRACK_H + offset;
}
```

- シナリオに登場する路線だけトラックを描く（不要な路線は非表示）

**同日衝突の回避**:
- 同じ x ± 5px 以内の駅をグループ化
- グループ内で y を ±12px ずらす
- ラベルは交互に上下配置

#### 4.4.3 駅の描画

| 状態 | SVG 表現 |
|---|---|
| `required` | 塗りつぶし円（r=7, fill=路線色, stroke=white） |
| `maybe` | 破線円（r=6, fill=none, stroke=路線色, stroke-dasharray="3 2"） |
| `skip` | 取り消し線つき小円（r=5, opacity=0.3） |
| `hidden` | 極小円（r=4, opacity=0.15）— 「不要」も安心情報 |
| `irreversible` | 赤リング追加（stroke=#E24B4A, stroke-width=2.5） |

**タップ/クリック**: 駅をタップ → 駅詳細パネル

### 4.5 駅詳細パネル

**目的**: 1つの手続の期限・必要書類・窓口・出典を確認する。
路線図から駅をタップして開く。跑腿プランナーの手続名タップでも開ける。

**表示方法**:
- デスクトップ: 右スライドインパネル（幅 360px）
- モバイル: フルスクリーンオーバーレイ（「← 戻る」つき）

**構成**:

```
┌──────────────────────────────┐
│  ← 戻る                      │
│                               │
│  ● 相続の放棄の申述           │  ← 路線色ドット + 手続名
│    相続線                     │
│                               │
│  [取り返しがつかない] [法定3月] │  ← バッジ群
│                               │
│ ─── 期限 ─────────────────── │
│  満了日    2026年11月1日      │
│  残り      86日               │
│  起算      相続を知った日      │
│  (目安)                       │  ← dueAssumed の場合
│  見逃すと  債務を含めて…       │
│                               │
│ ─── 必要書類 ────────────── │
│  戸籍謄本        [原本提出]    │
│  住民票の除票    [要確認]      │
│  本人確認書類    [提示のみ]    │
│                               │
│ ─── 行き先 ──────────────── │
│  管轄    家庭裁判所            │
│  課・係  (自治体選択時のみ)    │
│  電話    (自治体選択時のみ)    │
│                               │
│ ─── 前にやること ──────────── │
│  ● 死亡届の提出               │  ← depends_on。タップで遷移
│                               │
│ ─── 出典 ─────────────────── │
│  法令: 民法915条              │
│  出典: [リンク]                │
│  備考: ...                    │
└──────────────────────────────┘
```

**バッジの種類と色**:
| deadline.kind | ラベル | 背景色 | 文字色 |
|---|---|---|---|
| `irreversible` | 取り返しがつかない | #FCEBEB | #791F1F |
| `before_action` | 行動の前に | #FAEEDA | #633806 |
| `legal` | 法定期限 N日/月 | #E6F1FB | #0C447C |
| `claim` | 請求が必要 | #EAF3DE | #27500A |
| `practical` | 早めが望ましい | surface-0 | secondary |
| `none` | 期限なし | surface-0 | muted |

**requires_detail のバッジ**:
| 値 | ラベル | 背景色 |
|---|---|---|
| `submit` | 原本提出 | #E6F1FB |
| `show` / `copy` | 提示のみ | surface-0 |
| 未定義 | 要確認 | #FAEEDA |

---

## 5. 路線色定義

`procedures.json` の `lines[]` から引用:

```css
:root {
  --line-yakusho:  #E60012;  /* 役所線 — 赤 */
  --line-hoken:    #009944;  /* 保険線 — 緑 */
  --line-nenkin:   #F39800;  /* 年金線 — 橙 */
  --line-zeimu:    #0068B7;  /* 税金線 — 青 */
  --line-sozoku:   #920783;  /* 相続線 — 紫 */
  --line-minkan:   #7D7D7D;  /* 民間線 — 灰 */
  --line-zairyu:   #00A0E9;  /* 在留線 — 水色 */
  --line-shokuba:  #8FC31F;  /* 職場線 — 黄緑 */
}
```

---

## 6. レスポンシブ

| 幅 | 対応 |
|---|---|
| ≥768px (デスクトップ) | 2列グリッド可 / 路線図+右パネル / 余裕のある余白 |
| 375〜767px (モバイル) | 1列 / 路線図は横スクロール(min-width:600px) / 詳細はフルスクリーン |
| ≤374px | 375px と同様。フォント最小12px |

路線図 SVG:
- viewBox: `0 0 680 {tracks * 50 + 60}`
- コンテナ: `overflow-x: auto`
- モバイル: 「スワイプで全体を見る」ヒント表示

---

## 7. 印刷 (A4)

`@media print` で:
- ヘッダー/フッターのボタンを非表示
- 跑腿プランナー（Trip カード群 + 書類セクション）だけ表示
- フォント: 明朝系フォールバック
- A4 1枚に収まるよう font-size と margin を調整
- シナリオ名・日付を印刷ヘッダーに（自治体選択時は自治体名も）
- 「てつづきメトロ」ロゴと URL をフッターに
- 路線図は印刷しない

---

## 8. 言語切替

### 8.1 データ側

全テキストフィールドが `{ ja, easy, en }` の3キーを持つ。

### 8.2 UI 側

UI ラベルは `js/i18n.js` に集約:

```js
export const UI = {
  next:          { ja: '次へ',         easy: 'つぎへ',         en: 'Next' },
  back:          { ja: '戻る',         easy: 'もどる',         en: 'Back' },
  skip:          { ja: 'スキップ',     easy: 'とばす',         en: 'Skip' },
  clear:         { ja: '回答を消去',   easy: 'こたえを けす',  en: 'Clear answers' },
  print:         { ja: 'A4に印刷',     easy: 'いんさつ',       en: 'Print A4' },
  your_ward:     { ja: 'あなたの区役所', easy: 'あなたの やくしょ', en: 'Your ward office' },
  tab_trips:     { ja: 'やること',     easy: 'やること',       en: 'What to do' },
  tab_map:       { ja: '全体を見る',   easy: 'ぜんたい',       en: 'Timeline' },
  bring:         { ja: '持っていくもの', easy: 'もっていく もの', en: 'Bring with you' },
  trip_n:        { ja: '行き先',       easy: 'いきさき',       en: 'Trip' },
  // ...
};
```

### 8.3 切替ロジック

- 言語はハッシュの `&lang=` パラメータに保存
- `document.documentElement.lang` も切替
- やさしい日本語モードでは `font-size` を +2px

---

## 9. アクセシビリティ

- 全ボタンに `aria-label`
- SVG の駅: `role="button"` + `aria-label="{手続名} {路線名} {残りN日}"`
- 色だけでなく形状でも状態区別（required=塗り / maybe=破線 / hidden=極小）
- フォーカスリング: `outline: 3px solid` + 路線色
- 高コントラスト: 背景白 + テキスト黒 + 路線色は WCAG AA 以上
- `prefers-reduced-motion` でアニメーション無効化

---

## 10. プライバシー表示

結果画面のフッターに常時表示:

> あなたの回答は**この端末の URL にのみ保存**され、サーバーには一切送信されません。
> 「回答を消去」ボタンで URL も含めて完全に消去されます。
> 共有 PC をお使いの方は、終了時に消去してください。

---

## 11. SVG スパイク（最優先の技術検証）

HANDOFF.md が指摘する最大リスク。**ハッカソン当日より前に確認**:

### 11.1 検証項目

1. **14日クラスタ**: 死別シナリオの14日期限に最大5駅 → 縦 ±12px ずらし
2. **ラベル衝突**: BBox 計算で重なり検出 → ホバー/タップのみ表示
3. **期限超過**: `days <= 0` → 今日線の左に赤背景
4. **期限なし**: 右端に「期限なし」レーン
5. **トラック数変動**: viewBox.height を動的計算

### 11.2 スパイクの成果物

`js/screens/routemap.js` の座標計算関数だけ先に実装し、死別26駅で
「丸 + ラベルだけの版」を出力。デザインは後、座標が先。

---

## 12. 切り捨て順（時間がないとき）

この順で捨てる:

1. 民間の手続詳細（窓口情報が自治体ファイルにない）
2. 英語（EN）
3. Leaflet 地図（住所・電話のテキストで代替）
4. 印刷 CSS の作り込み（ブラウザのプレビューで済ませる）
5. 路線図のアニメーション

**絶対に捨てない**:
- 跑腿プランナー（Trip カード群）
- 戸籍謄本の通数計算
- 5つの質問
- 回答消去ボタン
- irreversible バナー
- 路線図 SVG（副画面としてでも残す — 審査員向けの見せ場）
