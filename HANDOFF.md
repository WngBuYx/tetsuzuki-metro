# HANDOFF — 実装引き継ぎ書

> 対象読者: これから UI を実装する人（W が連れてくる協力者、または当日の W 自身）。
> このファイルだけ読めば、会話の履歴なしで作業に入れることを目指す。
> 締切: **2026-08-23 17:00 提出**（ハッカソン本体 8/22-23、First Stage 動画収録 8/26-31）。

---

## 0. いまどこまで出来ているか

| 層 | 状態 |
|---|---|
| データ（69手続/40書類/36条件/7シナリオ/2自治体） | ✅ 完成。`npm run validate` が整合性を保証 |
| 経路計算エンジン `src/route.js` | ✅ 完成・テスト13件（`npm test`）。暦ベース期限・通数の幅計算・旧新住所地の分離まで対応済み |
| 動作確認 CLI | ✅ `npm run route` |
| **Web UI** | ❌ **未着手 ← あなたの仕事はこれ** |
| 発表素材 | 下書きあり（`PRESENTATION.md`）。スクショ・動画は UI 完成後 |

依存ライブラリ無し（地図に Leaflet を足すのは可）。ホスティングは Cloudflare Pages（特典）。

## 1. エンジンの使い方（これが UI の全 API）

```js
import { buildPlan } from './src/route.js';

const plan = buildPlan(
  db,             // { procedures, documents, conditions, scenarios } ← data/ の4ファイルを読むだけ
  'death',        // シナリオID
  answers,        // 質問への回答。空でも動く（未回答＝「不明」でグレー表示）
  shinjuku,       // data/municipalities/131041_shinjuku.json（無ければ null）
  new Date(),     // 今日
  { death: new Date('2026-08-01') }  // イベント日。deadline.from のキーに対応
);
```

`plan` に UI が必要とする全てが入っている:

| フィールド | 使い方 |
|---|---|
| `stations[]` | 全駅。`status`: required / maybe(グレー) / skip(急行通過) / hidden(薄く残す) |
| `timeline[]` | 路線図の横軸。`days`(残り日数) / `due`(満了日) / **`dueAssumed`(true なら「起算日は仮置き」と表示)** / `severity` |
| `documents[]` | 持ち物リスト。`copies`〜`copiesMax`(幅がある=未確認あり) / **`unconfirmedAt[]`(「要確認」と表示)** / `alternative`(法定相続情報一覧図の提案) |
| `windows[]` | 窓口ごとのまとめ。`jurisdictionLabel`(「旧住所地」等、空なら表示不要) / `oneTripPossible` / `depts[]`(課・係・電話) |
| `traps[]` | 「書類取得が期限に間に合わない」警告。そのまま表示 |
| `irreversible[]` | 取り返しがつかない期限。最上部に出す |

**UI 側で必ずやること:**
- 死別シナリオでは最初に**死亡日を1つ入力してもらう**（`eventDates.death`。`known`(知った日)・`funeral` は同日で初期化し、詳細設定で上書き可にする）。これが無いと全期限が「仮置き」になる
- `dueAssumed === true` の期限には「目安」と明示する
- 期限の暦計算は閉庁日の繰り延べ未対応 → 画面のどこかに免責を出す（文言は README §6.5）

## 2. 作る画面（4つ・優先順）

1. **入口**: シナリオ選択 → 質問（1画面1問・スキップ可）＋死亡日入力。言語切替(ja/easy/en)は常時右上
2. **路線図 (SVG)**: x=期限までの日数(対数)、y=路線。今日の位置に縦線。required=実線・maybe=グレー・hidden=薄く残す(「自分には不要」も安心情報)
3. **駅詳細**: 期限(満了日+残り日数+目安フラグ) / 必要書類(原本提出・提示のみ・**要確認**の3値) / 窓口(課・係・電話) / 出典リンク / 前提の駅
4. **持ち物リスト**: 通数(幅表示「1〜5通・うち要確認4」) / 窓口ごとのまとめ / **A4印刷**(1枚)

状態は URL ハッシュにのみ保持（サーバに送らない）。**「回答を消去」ボタンを必ず付ける**
——ハッシュと画面を初期化するだけ。遺族の回答(資産・借金の有無)が共有PCや履歴に残るのを防ぐ。

### SVG スパイク（最優先・当日より前に）

最大の技術リスク。死別26駅の大半が前半2週間に密集し、対数軸の左端で潰れる可能性が高い。
先に「座標計算＋丸＋ラベルだけの醜い版」を作って以下を確かめる:
- 同日期限の駅の縦ずらし（同一 x に最大5駅来る）
- ラベルの衝突回避（横に逃がすか、ホバー/タップで出すか）
- `days<=0`（期限超過）と期限なし駅の置き場所（右端に「期限なし」レーンを切る）

### 切り捨て順（時間が無いとき、この順で捨てる）

民間線の詳細 → 英語 → Leaflet地図(住所・電話のテキストで代替) → 印刷CSSの作り込み。
**絶対に捨てない**: 路線図・戸籍謄本の通数・5つの質問・回答消去ボタン。

## 3. 検収基準（これが通ったら「できた」）

- [ ] `npm test` / `npm run validate` が通る（データを触ったら `npm run audit` も再実行）
- [ ] 質問に全部答えない状態でも路線図が出る（3値論理の見せ場）
- [ ] 死亡日を入れると期限が暦で再計算される（8/1死亡→相続放棄は11/1満了、90日後ではない）
- [ ] 持ち物リストに「要確認」が正直に出る（勝手に少なく見積もらない）
- [ ] スマホ幅(375px)で操作できる／文字サイズは高齢者前提で大きめ・高コントラスト
- [ ] **実際のプリンタでA4に1枚で印刷して読める**（ブラウザのプレビューだけで済ませない）
- [ ] 回答消去ボタンで URL ハッシュも消える

## 4. データ側の残作業（UI と並行可）

作業キューは **`EVIDENCE_TODO.md`**（`npm run audit` で自動再生成）。現状:
根拠法令なし64 / 非官公庁出典13 / 提出方法未確認116組 / 書類出典なし38 / リードタイム不明9。
優先順: ①主線(死別)の駅の提出方法確認 → ②死別の駅の法令 → ③商用出典の差し替え。他シナリオは後回し。

既知の注意点:
- `data/procedures_zairyu.json` は主ファイルに統合済みの**遺留中間ファイル**。読み込むな（削除は W の承認待ち）
- `jidou-teate-moving` は転入先の認定請求のみモデル化。**旧住所地の消滅届が駅として欠けている**
- 引っ越しシナリオの自治体ファイルは「モデル区が新住所地/旧住所地のどちら側か」を区別しない（デモでは新宿区=転入先として見せる）
- おくやみガイドブックPDF: 再配布可否が未確認（README §6.5）。**公開リポジトリに含めない**

## 5. 歩き通し（提出前に3回やる）

1. **遺族役**: 死亡日を入れて質問に答え、持ち物リストを印刷し、紙だけ持って手順を口頭で説明できるか
2. **外国人住民役**: easy/en に切り替えて来日・引っ越しを一巡（訳の粗はメモして PRESENTATION の「今後」へ）
3. **窓口職員役**: 持ち物リストを見て「この書類でこの手続は受けられるか」を出典リンクから検証

## 6. 日程（外部制約）

| 日 | こと |
|---|---|
| 〜8/10ごろ | ⚠ **Cloudflare Pages 特典の招待メール**が届き次第 **3日以内に承認**（逃すと次バッチまで1週間） |
| 8/19 | 生成AI上級ハンズオン（案内は Slack） |
| 〜8/21 | SVGスパイク完了・スライド骨格完成（`PRESENTATION.md`）・データ主線ぶんの証拠固め |
| 8/22 | 午前: 雛形+JSON読込+質問UI ／ 午後: SVG路線図 ／ 夜: 駅詳細+持ち物リスト |
| 8/23 | 午前: 印刷+言語切替 ／ **13:00 機能凍結** ／ 14:00 デプロイ+スライド ／ **17:00 提出＋動画収録枠の予約（早い者順）** |
| 8/26-31 | First Stage 動画収録（2分。台本は `PRESENTATION.md`） |
